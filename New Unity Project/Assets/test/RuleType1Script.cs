using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RuleType1Script : RuleScript
{
    public new void Setup(EnumRules r, string label) {
        base.Setup(r, label);

    }
    // Start is called before the first frame update
    void Start()
    {
        base.Start();
    }

}
